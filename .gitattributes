# 统一 LF 入库。
# ⚠️ 关键：Dockerfile 如果是 CRLF 行尾，在云托管（Linux）里构建会直接报错，
#    典型症状是 "no such file or directory" 或指令解析异常，且很难看出是行尾问题。
* text=auto eol=lf

# 二进制文件不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
