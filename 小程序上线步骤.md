# 每日问候小程序 · 微信上线操作步骤

> 路线：**微信云托管** + **callContainer 私有协议**
> 这条路线**不需要买域名、不需要域名备案、不需要自备 HTTPS 证书**，是最快的上线方式。

---

## 现状盘点

本地代码侧已完成，不需要你操作：

- [x] 后端 `server/Dockerfile`（监听 80）+ `.dockerignore` + `.gitattributes`（强制 LF）
- [x] 小程序端双通道改造：`http`（本地联调）/ `container`（云托管）
- [x] 本地 Git 仓库初始化 + 首次提交（`ea3d940`）
- [x] Supabase 建表完成：语料 64 条、2026 节假日 41 条

需要你在浏览器 / 微信后台操作的：

- [x] 拿 AppID（已填 `wxb75bfb119207866c`）
- [x] 代码推送到远程仓库（`github.com/xhm198811/myDailyWisdom365tool` main 分支，38 个文件）
- [ ] 云托管建服务 + 配环境变量 ← **下一步**
- [ ] 分配流量、记录环境 ID
- [ ] 填回 `config.js`
- [ ] 小程序 ICP 备案
- [ ] 上传 → 体验版 → 提交审核 → 发布

---

## 步骤 1 · 拿 AppID

1. 打开 <https://mp.weixin.qq.com>，微信扫码登录
2. 左侧「开发」→「开发管理」→「开发设置」
3. 复制 **AppID(小程序ID)**

把它填进 `miniprogram/project.config.json` 的 `appid` 字段（当前是 `touristappid`，游客模式没法上传代码）。

---

## 步骤 2 · 推送代码到远程仓库 ✅ 已完成

代码已在 `github.com/xhm198811/myDailyWisdom365tool` 的 `main` 分支。

> ⚠️ **踩坑记录**：这台机器上 GitHub 的 **443 端口出不去** —— 走代理报 `502 CONNECT tunnel failed`，
> 直连 21 秒超时。但 **SSH 的 22 端口是通的**，所以 remote 用的是 SSH 地址而非 HTTPS：
> ```
> origin  git@github.com:xhm198811/myDailyWisdom365tool.git
> ```
> 换了机器如果遇到同样问题，先 `ssh -T git@github.com` 测一下，能通就改走 SSH。
>
> 已核验：远程 38 个文件中**无 `.env`**（数据库密码未泄露），Dockerfile 为纯 LF 行尾。

---

## 步骤 3 · 云托管建服务

微信云托管控制台 → 左侧「服务列表」→「新建服务」。

**基本配置**

| 项 | 填什么 |
|---|---|
| 服务名称 | `greeting-api` |
| 部署方式 | 从代码仓库部署 → 授权 → 选仓库 + 分支 `main` |
| **构建目录** | `server` ← 关键！Dockerfile 在子目录 |
| Dockerfile 路径 | `Dockerfile` |
| **容器端口** | `80` ← 必须和 Dockerfile 里的 EXPOSE 一致 |
| 健康检查路径 | `/api/v1/health` |
| 容器规格 | 0.25 核 0.5G（够用） |
| 实例数量 | 最小 `0` / 最大 `2` |

**环境变量**（逐条添加，等价于本地 `.env`）

```
APP_ENV=prod
SUPABASE_DB_HOST=aws-0-ap-southeast-1.pooler.supabase.com
SUPABASE_DB_PORT=5432
SUPABASE_DB_NAME=postgres
SUPABASE_DB_USER=postgres.acqnkgbovfhocoqdgtps
SUPABASE_DB_PASSWORD=<你的数据库密码>
SUPABASE_DB_SSLMODE=require-no-verify
DEFAULT_CITY=宁波
WEATHER_CACHE_TTL=600
```

点「创建并部署」，首次构建约 3-5 分钟。

> ⚠️ **不要开公网访问**。`callContainer` 走微信私有协议，不需要公网域名，开了反而多一个暴露面。
> ⚠️ 容器连不上 Supabase 时，检查云托管环境的出网配置（需要能访问公网）。

---

## 步骤 4 · 分配流量 + 记录两个值

1. 部署完成后点「**分配流量比例**」→ 拉到 `100%`
   **不分配流量的话服务永远返回 404**，这一步最容易漏
2. 记录两个值，下一步要用：
   - **环境 ID**：环境概览页，形如 `prod-xxxxxxxx`
   - **服务名称**：`greeting-api`

---

## 步骤 5 · 填回小程序配置

编辑 `miniprogram/config.js`：

```js
const ENV = 'prod' // ← 从 'dev' 改成 'prod'

prod: {
  channel: 'container',
  apiOrigin: '',
  apiPrefix: '/api/v1',
  cloudEnv: 'prod-xxxxxxxx',      // ← 换成你的环境 ID
  cloudService: 'greeting-api',   // ← 换成你的服务名
  requestTimeout: 10000,
},
```

---

## 步骤 6 · 开发者工具验证

1. 打开微信开发者工具 → 导入项目 → 目录选 `D:\MywxApp\miniprogram` → 填 AppID
2. 编译，看首页有没有数据
3. 控制台应打印 `[app] cloud container mode, env = prod-xxxxxxxx`

排查：若报 `callContainer` 相关错误，依次检查 —— 环境 ID 是否写对、服务名是否一致、小程序与云托管环境是否**同一主体**、基础库是否 ≥ 2.23.0（详情 → 本地设置 → 调试基础库）。

---

## 步骤 7 · 上传代码

开发者工具右上角「**上传**」→ 版本号填 `1.0.0` → 备注 → 上传。

上传成功后，mp 后台「管理」→「版本管理」会出现「开发版本」。

---

## 步骤 8 · 体验版实测（正式发布前的最后一次防线）

1. 版本管理 → 开发版本 → 「**选为体验版**」
2. 「体验成员」里加上你自己的微信号
3. 手机上打开体验版，实测这 4 件事：
   - 日期 / 农历 / 干支 / 节气是否正常显示
   - 天气卡片有内容（目前无和风 key，走的是 mock 假数据，卡片也该有内容）
   - 敬语有内容，下拉刷新能换
   - 点「换一句」能刷新

真机这一步 **wx.request 会被域名白名单拦住**，而 `callContainer` 不会——所以如果你看到数据正常，说明通道改造是对的。

---

## 步骤 9 · 小程序 ICP 备案（必须在提审前完成）

mp 后台 →「设置」→「基本设置」→「**小程序备案**」→ 按指引提交。

- 材料：主体证件、负责人身份证、人脸核验、应急联系人
- 耗时：一般 1-7 个工作日（各省通信管理局不同）

> ⚠️ 2023 年 9 月后的规定，**没完成备案提交审核会被直接驳回**。建议和第 3 步并行推进。

---

## 步骤 10 · 提交审核

版本管理 → 开发版本 →「提交审核」。

| 项 | 建议填法 |
|---|---|
| 主营类目 | 工具 → 效率（或 工具 → 日历） |
| 功能页面 | `pages/index/index` |
| 功能描述 | 展示每日问候语、农历日历、节气与传统节日信息 |
| 测试账号 | 无需登录，可留空 |

> ⚠️ **类目提醒**：不要选需要资质的「天气」类目。页面里含天气卡片是个风险点，若审核以「气象服务需资质」驳回，**最快的处理是把天气模块隐藏或改成「生活提示」文案后重新提交**，比申请资质快得多。

审核时长一般 1-7 天，多数 1-2 天出结果。

---

## 步骤 11 · 发布

审核通过 → 版本管理 → 审核版本 →「**发布**」。

发布后正式版对所有用户可见，同时可以下载小程序码用于推广。

---

## 常见坑速查

| 症状 | 真正原因 |
|---|---|
| 部署成功但访问 404 | 版本没分配流量（步骤 4 漏了） |
| 容器访问超时 | 容器端口不是 80，或健康检查路径写错 |
| 真机报 `url not in domain list` | 用了 `wx.request` 打云托管默认域名——该域名未备案填不进白名单，必须用 `callContainer` |
| `callContainer` 报环境不存在 | envId 填错，或小程序与云托管环境不是同一主体 |
| 容器连不上 Supabase | 云托管出网受限，检查环境网络配置 |
| 首屏慢几秒 | 实例最小数 0 触发冷启动，改成 1 可解决（会产生费用） |
| 构建报 `no such file or directory` | Dockerfile 是 CRLF 行尾（本项目已用 `.gitattributes` 强制 LF，不会踩） |
| 数据库一直是 `down` | 直连 `db.<ref>.supabase.co` 是 IPv6-only，必须走 pooler（见 `server/.env.example`） |
